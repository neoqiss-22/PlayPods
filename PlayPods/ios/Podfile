# Example Podfile placeholder for PlayPods (iOS)
# Replace with the Podfile generated by your iOS project (e.g., from React Native or Xcode)

platform :ios, '12.0'

target 'PlayPods' do
  # Uncomment and add pods as needed, for example:
  # pod 'AFNetworking', '~> 4.0'

  # use_frameworks! # enable if your project requires frameworks
  # Local native module: FeedView
  pod 'FeedView', :path => '../native-modules/FeedView'
end
