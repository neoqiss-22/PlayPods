# Minimal CMakeLists.txt placeholder for Windows native build
cmake_minimum_required(VERSION 3.15)
project(PlayPods)

# Example: add an executable (replace sources with real files)
# add_executable(PlayPods WIN32 src/main.cpp)

# Link Windows libraries or vcpkg targets as needed
